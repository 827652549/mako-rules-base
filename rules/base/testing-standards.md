# 测试规范（通用原则）

> 平台专属测试框架和示例见各平台规范。

## 核心原则

- 测试文件与源文件放在同一目录，命名格式为 `*.test.*` 或 `*.spec.*`
- 公共测试工具放在 `tests/utils/`，测试数据放在 `tests/fixtures/`
- 使用 `describe` / `it` 清晰描述场景，优先从用户行为角度命名（「用户点击提交后显示错误」）
- 编写测试前先考虑边界条件（空值、越界、并发、网络失败）
- 禁止测试依赖执行顺序，每个 case 必须可独立运行
