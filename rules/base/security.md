# 安全规范（通用）

## 密钥与环境变量
- 所有密钥、token、数据库连接串禁止硬编码在源码中
- 敏感配置文件必须在 `.gitignore` 中，禁止提交到版本库
- 新增环境变量必须同步更新示例配置文件（仅保留 key，value 留空或填示例值）

## 输入校验
- 所有来自用户或外部系统的输入，必须在边界处校验，内部函数不重复校验
- 使用平台对应的校验库，禁止手写正则替代完整的结构校验

## 依赖安全
- 定期使用包管理器的审计命令检查依赖漏洞，修复高危项
- 不引入无维护者或下载量极低的包
